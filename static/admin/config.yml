backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)
# This line should *not* be indented
media_folder: "static/uploads" # Media files will be stored in the repo under images/uploads
collections:
  - name: "blog-en" # Used in routes, e.g., /admin/collections/blog
    label: "Blog EN" # Used in the UI
    folder: "content/blog" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}.{{i18nlanguage}}" # Filename template, e.g., YYYY-MM-DD-title.md
    extension: "en.md"
    format: 'frontmatter'
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Draft", name: "draft", widget: "boolean"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Description", name: "description", widget: "string"}
      - {label: "Preview Image", name: "image_preview", widget: "image"}
      - {label: "Main Image", name: "image_main", widget: "image"}
      - {label: "Author", name: "author", widget: "select", options: ["martin_hubacek","pavel_hubner","michal_muhlpachr","alan_fabik","lukas_fabik"]}
      - {label: "Body", name: "body", widget: "markdown"}
  - name: "blog-cs" # Used in routes, e.g., /admin/collections/blog
    label: "Blog CS" # Used in the UI
    folder: "content/blog" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}.{{i18nlanguage}}" # Filename template, e.g., YYYY-MM-DD-title.md
    extension: "cs.md"
    format: 'frontmatter'
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Draft", name: "draft", widget: "boolean"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Description", name: "description", widget: "string"}
      - {label: "Preview Image", name: "image_preview", widget: "image"}
      - {label: "Main Image", name: "image_main", widget: "image"}
      - {label: "Author", name: "author", widget: "select", options: ["martin_hubacek","pavel_hubner","michal_muhlpachr","alan_fabik","lukas_fabik"]}
      - {label: "Body", name: "body", widget: "markdown"}
  - name: "projects-en" # Used in routes, e.g., /admin/collections/blog
    label: "Projects EN" # Used in the UI
    folder: "content/projects" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    extension: "en.md"
    format: 'frontmatter'
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Draft", name: "draft", widget: "boolean"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Description", name: "description", widget: "string"}
      - {label: "Environment", name: "tags", widget: "select", options: ["Indoor", "Outdoor"], multiple: true}
      - {label: "Knowledge", name: "levels", widget: "select", options: ["Beginner","Advanced"], multiple: true}
      - {label: "Place", name: "places", widget: "select", options: ["Home","Garden","Office"], multiple: true}
      - {label: "Device", name: "devices", widget: "select", options: ["Custom Kit", "Motion Kit", "Flood Kit", "Power Kit"], multiple: true}
      - {label: "Idea", name: "idea", widget: "boolean"}
      - {label: "Preview Image", name: "image_preview", widget: "image"}
      - {label: "Main Image", name: "image_main", widget: "image"}
      - {label: "Author", name: "author", widget: "select", options: ["martin_hubacek","pavel_hubner","michal_muhlpachr","alan_fabik","lukas_fabik"]}
      - {label: "Body", name: "body", widget: "markdown"}
  - name: "projects-cs" # Used in routes, e.g., /admin/collections/blog
    label: "Projects CS" # Used in the UI
    folder: "content/projects" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    extension: "cs.md"
    format: 'frontmatter'
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Draft", name: "draft", widget: "boolean"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Description", name: "description", widget: "string"}
      - {label: "Environment", name: "tags", widget: "select", options: ["Indoor", "Outdoor"], multiple: true}
      - {label: "Knowledge", name: "levels", widget: "select", options: ["Beginner","Advanced"], multiple: true}
      - {label: "Place", name: "places", widget: "select", options: ["Home","Garden","Office"], multiple: true}
      - {label: "Device", name: "devices", widget: "select", options: ["Custom Kit", "Motion Kit", "Flood Kit", "Power Kit"], multiple: true}
      - {label: "Idea", name: "idea", widget: "boolean"}
      - {label: "Preview Image", name: "image_preview", widget: "image"}
      - {label: "Main Image", name: "image_main", widget: "image"}
      - {label: "Author", name: "author", widget: "select", options: ["martin_hubacek","pavel_hubner","michal_muhlpachr","alan_fabik","lukas_fabik"]}
      - {label: "Body", name: "body", widget: "markdown"}
